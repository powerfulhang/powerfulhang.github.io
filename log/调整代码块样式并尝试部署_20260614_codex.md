# 调整代码块样式并尝试部署

- 日期：2026-06-14
- 工具：Codex
- 项目根目录：F:\Working Files\Coding\PersonalWebsite

## 本次修改

- `assets/css/extended/custom.css`: 参考用户提供的代码块截图，重做代码块面板样式，使行号列和代码内容位于同一整体面板中，统一行高、内边距、缩进和列对齐。
- `assets/css/extended/custom.css`: 增加代码块明亮/黑暗主题变量，并补充 `:root[data-theme="dark"]` 选择器以匹配 PaperMod 当前主题切换方式。
- GitHub 认证部署: 按用户要求执行 `gh auth setup-git`，但当前 `gh` 未登录，命令返回需要先运行 `gh auth login`。

## 验证

- 运行 `hugo --minify --cleanDestinationDir`，构建成功。
- 构建输出仍有 Hugo 既有配置弃用警告：`languageCode`、`.Language.LanguageDirection`、`.Language.LanguageCode`。

## 备注

- 当前 GitHub CLI 状态为未登录；如果后续 push 仍失败，需要先完成 `gh auth login` 或恢复可用的 GitHub 凭据。
