# 重建 IC 服务器文章并修复代码块

- 日期：2026-06-14
- 工具：Codex
- 项目根目录：F:\Working Files\Coding\PersonalWebsite

## 本次修改

- `assets/css/extended/custom.css`: 调整代码块外边距、内边距、行高、行号列宽度和 Chroma 表格垂直对齐，改善代码块上下留白和行号/代码内容对齐。
- `content/posts/`: 删除旧的 IC 服务器短文章和测试文章，基于 `F:\Temp Files\IC服务器配置流程.docx` 重新拆分并生成 4 篇可独立阅读的文章：基础环境初始化、NFS 共享 Home、NIS 用户集中管理、IBM LSF 集群配置。

## 验证

- 运行 `hugo --minify --cleanDestinationDir`，构建成功。
- 检查 `public/posts`，确认仅生成 4 篇新 IC 服务器文章目录。
- 检查 `public/index.html`，确认首页文章列表显示 4 篇新文章。
- 检查生成后的 CSS，确认代码块对齐相关规则已进入最终 stylesheet。
- 参考线上 `https://powerfulhang.github.io/`，确认当前部署版本仍为旧短文章，等待本次修改推送部署后更新。

## 备注

- Hugo 输出 `languageCode`、`.Language.LanguageDirection`、`.Language.LanguageCode` 的弃用警告，属于既有配置与新版 Hugo 的兼容提示，本次未扩展修改范围。
